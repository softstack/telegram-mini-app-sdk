/**
 * Represents the supported operating systems.
 *
 * @typedef {('android' | 'ios')} OperatingSystem
 *
 * @example
 * // Example usage:
 * const os: OperatingSystem = 'android';
 */
export type OperatingSystem = 'android' | 'ios';
